# include what you want
# add this to the .bashrc: . $HOME/.bashrc.load

export PATH=$PATH:$HOME/bin:$HOME/bin/git-annex.linux
export EDITOR=vim

####
# put any bash customizations in here
 .  /etc/profile.d/autojump.bash
 . .bashprompt
 eval "$(dircolors ~/.dir_colors)"

alias ll='ls -alF'
alias la='ls -A'
